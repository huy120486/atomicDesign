import React from 'react';
import {
  Spinner,
} from '../../react-velonic';

const SpinnerComponent = () => (
  <div>
    <Spinner spinnerStyle={{ color: 'black' }} />
  </div>
);

export default SpinnerComponent;
